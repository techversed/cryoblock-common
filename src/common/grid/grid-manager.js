angular.module('grid.gridManager', [])

    .factory('gridManager', [

        function () {

            var GridManager = {
                ignoreUrlParams: false,
            };

            return GridManager;
        }

    ])
;
